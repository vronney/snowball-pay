# ── Database ────────────────────────────────────────────────────────────────
# Pooled connection URL (used at runtime — Neon: use the -pooler endpoint)
DATABASE_URL="postgresql://neondb_owner:npg_qCUDA3Ncirh1@ep-withered-heart-anh0oxog-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Direct (non-pooled) connection URL (used by Prisma Migrate)
# Neon: same as above but without the -pooler subdomain
DIRECT_URL="postgresql://neondb_owner:npg_qCUDA3Ncirh1@ep-withered-heart-anh0oxog-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# ── Auth0 ────────────────────────────────────────────────────────────────────
# Your Auth0 application domain
AUTH0_DOMAIN="your-tenant.us.auth0.com"

# Auth0 application Client ID and Secret
AUTH0_CLIENT_ID="your_client_id"
AUTH0_CLIENT_SECRET="your_client_secret"

# 64-character secret used to encrypt the session cookie
# Generate with: openssl rand -hex 32
AUTH0_SECRET="replace_with_64_char_random_hex"

# The base URL of your deployed app (no trailing slash)
# On Vercel this is your production domain, e.g. https://snowballpay.vercel.app
APP_BASE_URL="https://your-app.vercel.app"

# ── Anthropic ────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY="sk-ant-..."
