// Application-wide constants

export const EMAIL_FROM = 'SnowballPay <noreply@getsnowballpay.com>';
// Where "just reply to this email" actually lands. The from address is a
// no-reply mailbox, so any email that asks for a reply must set this.
export const SUPPORT_EMAIL = 'support@getsnowballpay.com';
export const APP_BASE_URL = 'https://getsnowballpay.com';

// Cron and pagination
export const CRON_PAGE_SIZE = 25;

// Date helpers
export const MAX_STREAK_MONTHS = 24;
