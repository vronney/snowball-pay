"use client";
// HelpTab is not rendered in the current nav. Kept as a stub.
export default function HelpTab({ onNavigate }: { onNavigate?: (tab: string) => void }) { 
  void onNavigate;
  return null; 
}
